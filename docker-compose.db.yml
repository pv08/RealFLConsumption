services:
  mongodb:
    image: mongo
    container_name: fl_mongo
    restart: always
    ports:
    - 27017:27017
    volumes:
    - ./mongo_data:/data/db
    networks:
    - fl-network

networks:
  fl-network:
    external: true
    driver: bridge
